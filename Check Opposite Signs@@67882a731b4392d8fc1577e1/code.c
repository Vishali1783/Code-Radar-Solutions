#include<stdio.h>
int main()
{
    int a,b;
    scanf("%d %d",&a,&b);
    if((a=+ve && b==-ve) || (a=-ve && b=+ve)){
        printf("True");
    }
    else{
        printf("False");
    }
    return 0;
}